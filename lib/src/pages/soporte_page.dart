import 'package:flutter/material.dart';

class SoportePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Soporte Page'),
    );
  }
}